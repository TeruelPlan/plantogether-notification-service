package com.plantogether.notification.security;

import com.plantogether.common.security.AbstractDeviceIdFilterTest;

/** Security regression test — verifies DeviceIdFilter is active in notification-service (R01). */
class DeviceIdFilterTest extends AbstractDeviceIdFilterTest {}
